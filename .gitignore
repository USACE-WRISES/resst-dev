node_modules/
dist/
test-results/
playwright-report/
.env
.env.*
*.local
Thumbs.db
.DS_Store
.claude/scheduled_tasks.lock

# Raw sedimentation-dataset downloads (multi-GB; one RATTES CSV exceeds GitHub's
# 100 MB hard limit). Re-fetch instructions + hashes: data/DATA-SOURCES.md.
data/rattes/
data/resnet/
data/ressed/

# Posit Connect Cloud test mirror, built by `npm run build:connect`. Only Posit
# Publisher's configuration and deployment record under .posit/ are tracked.
connect-cloud/*
!connect-cloud/.posit/
